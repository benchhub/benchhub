# benchhubtype=go

package: github.com/benchhub/benchhub/_example/sort
server:
  name: local # unique name on client side, not send to server
  addr: localhost:1124
command:
  command: make bench # the output is saved
  output: bench.txt
report:
  input: bench.txt